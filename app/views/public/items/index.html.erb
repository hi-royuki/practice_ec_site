<h1>商品一覧</h1>
<% @items.each do |item| %>
<div>
  <%= link_to admins_item_path(item.id) do %>
   <%= attachment_image_tag item, :image %>
  <% end %>
   <p>商品名：<%= item.item_name %></p>
   <p>税抜価格：<%= item.no_tax_price %></p>
   <p>商品説明:<%= item.introduction %></p>
   <% if item.is_active? %>
    <p>販売停止</p>
   <% else %>
    <p>販売中</p>
   <% end %>
</div>
<% end %>